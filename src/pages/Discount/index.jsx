import React from "react";
import { CartState } from "../../context/Auth";

const index = () => {
  // const {
  //   state: { oneCart },
  // } = CartState();
  // console.log(oneCart);
  return (
    <>
      <div>discount {}</div>
      {/* {oneCart.map((e) => {
        <h3>{e.title}</h3>;
      })} */}
    </>
  );
};

export default index;
